spring.application.name=studville

server.port=9090

    # application.properties
    spring.datasource.url=jdbc:postgresql://localhost:5432/stud_ville
    spring.datasource.username=postgres
    spring.datasource.password=justicechidi
    spring.datasource.driver-class-name=org.postgresql.Driver

    # Optional: JPA/Hibernate configurations
    spring.jpa.hibernate.ddl-auto=update # or create, create-drop, validate, none
    spring.jpa.show-sql=true
    spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

